{
  "buildCommand": "npm run build",
  "installCommand": "npm ci --loglevel=error --no-audit --no-fund",
  "regions": ["sin1"],
  "functions": {
    "src/app/api/**": {
      "maxDuration": 30
    }
  }
}
